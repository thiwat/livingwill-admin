export enum ListItemType {
  string = 'string',
  date = 'date',
  badge = 'badge',
  action = 'action',
  boolean = 'boolean',
  number = 'number',
  image = 'image',
  drag = 'drag'
}


export enum FilterItemType {
  string = 'string',
  date = 'date',
  options = 'options',
  boolean = 'boolean'
}