export enum DetailItemType {
  space = 'space',
  string = 'string',
  boolean = 'boolean',
  password = 'password',
  select = 'select',
  number = 'number'
}

export enum ActionMode {
  create = 'create',
  update = 'update'
}