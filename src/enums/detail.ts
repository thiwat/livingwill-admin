export enum DetailItemType {
  boolean = 'boolean',
  checkbox = 'checkbox',
  space = 'space',
  string = 'string',
  password = 'password',
  datetime = 'datetime',
  select = 'select',
  wysiwyg = 'wysiwyg',
  number = 'number',
  json = 'json'
}

export enum ActionMode {
  create = 'create',
  update = 'update'
}

export enum AfterActionExecute {
  update = 'update'
}