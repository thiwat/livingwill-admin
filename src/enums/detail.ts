export enum DetailItemType {
  space = 'space',
  string = 'string',
  boolean = 'boolean',
  password = 'password',
  datetime = 'datetime',
  select = 'select',
  wysiwyg = 'wysiwyg',
  number = 'number'
}

export enum ActionMode {
  create = 'create',
  update = 'update'
}